# UX Polish Agent

> 负责 CurioSync 用户体验打磨的 Agent

## 职责范围

- 动画和过渡效果
- 空状态设计
- 错误处理和降级展示
- 响应式布局
- 交互细节优化

## 设计规范

### 颜色
```css
--primary: #1a56db;        /* 深蓝主色 */
--primary-light: #3b82f6;  /* 浅蓝 */
--background: #f9fafb;     /* 背景灰 */
--surface: #ffffff;        /* 卡片白 */
--text: #111827;           /* 主文字 */
--text-secondary: #6b7280; /* 次要文字 */
--success: #22c55e;        /* 成功绿 */
--warning: #f59e0b;        /* 警告黄 */
--error: #ef4444;          /* 错误红 */
```

### 动画时长
- 微交互: 150ms (ease-out)
- 展开/收起: 200ms (ease-in-out)
- 页面过渡: 300ms (ease-in-out)
- 加载动画: 持续循环

### 圆角
- 按钮: rounded-lg (8px)
- 卡片: rounded-xl (12px)
- 消息气泡: rounded-2xl (16px)
- 模态框: rounded-xl (12px)

## 待完成任务

### Phase 3 - 介入卡片
- [x] **ConvergeCard** (收敛聚焦)
  - 背景: 浅琥珀色 #fffbeb
  - 左边框: 3px 琥珀色 #f59e0b
  - 按钮样式: 琥珀色边框

- [x] **TransitionCard** (阶段跃迁)
  - 背景: 浅蓝色 #eff6ff
  - 左边框: 3px 蓝色 #3b82f6
  - 方向卡片点击 → 自动填入输入框并发送

- [x] **BookmarkToast** (书签提示)
  - 位置: 右下角浮动
  - 背景: 浅绿色 #f0f9f4
  - 边框: 绿色 #22c55e
  - 自动消失: 3秒

### Phase 4 - 体验打磨

#### 已完成优化
- [x] **转义字符处理**: `\\n` → 换行, `\\t` → tab
- [x] **AI 生成中指示器**: 消息气泡内显示"生成中" + 三个跳动圆点
- [x] **输入框状态提示**: 禁用时显示"AI 正在思考中..."
- [x] **图谱节点出现动画**: scale 0 → 1，400ms
- [x] **错误处理降级**: 加载失败显示错误提示 + 重试按钮

#### 对话标题自动生成
- [x] 第一条消息发送后异步生成
- [x] 生成后更新侧边栏标题

#### 图谱节点动画
- [x] 新节点出现: scale 0 → 1，400ms
- [ ] 节点高亮: pulse 效果（待实现）
- [ ] 边连接: draw line animation（待实现）

#### 侧边栏动画
- [x] 收起: 220px → 48px，200ms（已有基础动画）
- [x] 展开: 48px → 220px，200ms（已有基础动画）
- [ ] 状态保存 localStorage（待实现）

#### 空状态设计

- [x] 新对话空状态: "你好！这是关于[主题名]的对话"
- [x] 空知识图谱: "开始对话后，知识图谱会在这里自动生长 ✦"

#### 错误处理

- [x] 加载失败: 显示错误提示 + 重试按钮
- [x] LLM 失败: 显示"AI 回复失败，请稍后重试" + 重试
- [ ] 网络断开: 自动尝试重连（待实现）
- [ ] 消息发送失败: 显示重发按钮（待实现）

#### 响应式
- 最小支持宽度: 1024px (平板)
- 侧边栏在 < 1024px 时默认收起
- 知识图谱在小屏幕适配

## 组件状态

### 输入框
- 默认: 浅灰背景，placeholder 提示
- 聚焦: 蓝色边框 ring
- 禁用: 半透明，loading 动画
- 错误: 红色边框

### 消息气泡
- 用户: 右对齐，蓝色背景
- AI: 左对齐，灰色背景
- 流式输出: 末尾闪烁光标 ▌
- 加载中: 渐变背景动画

### 节点提示 Toast
- 位置: 对话区底部，输入框上方
- 显示时长: 3秒
- 最多显示: 3个节点
- 点击节点 → 跳转图谱并高亮

## 协作接口

### 给 Frontend Agent
- 提供 UI 实现规格
- 指定动画参数

### 给 Prompt Agent
- 反馈用户体验问题
- 建议介入时机调整

### 给 Test Agent
- 提供 UX 测试用例
- 报告交互问题

## 参考文档

- 产品概念: `docs/concept/CurioSync Concept v0.1.md`
- 开发规格: `docs/specs/CurioSync MVP Development Document v2.0.md` (第五章 UI 设计)
- 根配置: `CLAUDE.md`
